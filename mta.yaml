_schema-version: "3.2"
ID: CICDwithTest
version: 0.0.1
modules:
- name: cicdwithtest-approuter
  type: approuter.nodejs
  path: cicdwithtest-approuter
  requires:
  - name: CICDwithTest_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: CICDwithTest_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: CICDwithTest_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cicdhtml-content.zip
      name: cicdhtml
      target-path: resources/
- name: cicdhtml
  type: html5
  path: cicdhtml
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: CICDwithTest_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: CICDwithTest_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
